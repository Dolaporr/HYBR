# Playwright logs removed from the branch tip.

This README is a placeholder explaining that Playwright logs and related artifacts were removed from the branch tip and are now ignored by .gitignore.

If you need to retrieve the original logs they remain in the repository history. To purge them from history across all branches, run a history-rewrite tool such as the BFG or git filter-repo (this is a destructive change and requires coordination with collaborators).
